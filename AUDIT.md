# 上海康肯销售订单管理系统可用性与可靠性审计

审计范围：本地数据持久化、六表关系、Excel 导出、桌面 API、主要表格交互与 UI。优先级按“可能破坏数据 > 阻断业务 > 操作不便 > 视觉一致性”排序。

## 已修复的高风险问题

| 风险 | 原行为 | 修复 |
|---|---|---|
| 非事务 JSON 覆盖 | 保存过程中断可能留下不完整状态，且无关系约束 | SQLite 单事务保存、WAL、`synchronous=FULL`、写前快照、完整性与外键检查 |
| 跨表误删 | 切换标签页后保留上一表的行索引，删除可能作用于另一表同序号 | 切表立即清空选择与锚点；交互预览已验证 1 → 0 |
| 取消级联仍删除 | 取消两次确认后会退回为只删合同头，留下五表孤儿 | 合同页删除由级联流程独占；任一确认取消即零变更 |
| 重复主键 | “重复行”复制原 `记录ID` | 新增、粘贴、拆分、重复均生成新本地 ID；后端还会修复缺失/重复 ID |
| JOB/批次直接改名失联 | 改合同 JOB 或发货批次名称时子表不自动同步 | 编辑时同步六表 JOB；批次名同步设备及开票/回款覆盖批次；数据库外键二次保护 |
| 坏数据可保存 | 前端校验不完整，后端保存不拒绝业务错误 | 后端拒绝重复合同、缺失/孤儿 JOB、重复批次、无效枚举/日期/比例、不存在的设备批次与开票/回款覆盖关系 |
| 自动字段可被误编辑 | 无下拉选项的自动字段在前后端 schema 中标记错位 | schema 固定为“字段/类型/选项/自动”四位结构，自动列恢复只读 |
| 跨表查询漏查未回款 | 旧查询只能关联六张基础表，无法查询生成台账的未回收派生行；开票表的回款状态也不能覆盖未开票项 | 新增「未付款订单」与「未回款明细」虚拟数据源，统一调用 `compute_unpaid_rows` 口径 |
| Excel 回导覆盖数据库 | 外部表格缺少数据库约束，导入可能破坏关联或覆盖新版 | 取消桌面端 Excel 回导；数据库成为唯一权威源，XLSX 只能由数据库导出 |
| 多开覆盖新数据 | 另一应用窗口保存后，旧窗口仍可能覆盖新版 | 所有保存使用修订号乐观锁，旧修订保存会被阻断 |
| 业务修改无法追溯 | 原 `_change_log` 只有修订和总行数，无法定位字段前后值 | v1.5.0 增加启用基线、字段级追加审计、操作人/来源、同事务回滚、哈希链验证与 XLSX 导出 |
| Excel 公式注入 | `=` 开头业务文本会被 Excel 当公式 | 普通导出与台账生成均转义用户文本 |
| 无设备订单少算/消失 | 同款多笔开票回款仅显示首笔；缺比例时从未回收表静默跳过 | 多笔金额聚合、日期取最新；缺比例订单显示 `④待确认` 与可核对金额 |
| Windows 发布缺数据文件/路径失效 | 旧压缩包不含 DB/XLSX，设置路径只在当前会话生效 | Windows zip 内置可用数据库及导出文件；数据库路径可切换并保存，XLSX 路径自动派生 |
| 报价散落且历史价难查 | 报价只存在外部工作簿，客户/型号重复录入，无法可靠查询同客同机型价格 | 新增报价主表/明细表、事务保存与审计；从 JOB 带入已有字段，每条明细可查同客户同型号历史报价 |

## 数据库关系

- `合同订单.JOB No` 唯一。
- 五个子表以 `JOB No` 外键关联合同，更新级联、删除级联。
- `发货批次 (JOB No, 发货批次)` 唯一。
- `设备台账 (JOB No, 发货批次)` 关联发货批次；已有设备时不能单独删除其批次。
- 未直接建立外键的“款类”和“覆盖批次串”仍由业务校验与派生逻辑处理，因为现有业务允许全额开票、空占位和多批次覆盖；非空覆盖项必须命中同 JOB 的批次/製造番号。
- 不属于正式字段的远端关联/公式字段保存在每行 `_extra_json`，JSON 迁移时完整保留。
- `quotation.quote_id` 唯一，`quotation.quote_no` 唯一；`quotation_item.quote_id` 外键关联报价头并级联删除。历史查询基于客户与型号规范化精确匹配，不按模糊包含混入近似型号。

## 可用性改进

- 顶部明确区分“保存数据库 / 导出台账”，设置页只需要选择数据库。
- 摘要显示数据库修订、完整性和未保存状态；六表数量卡可直接跳转。
- 数据错误在保存时阻断并回到摘要，避免用户只看到底部短提示。
- 切换数据库前提示未保存更改；关闭窗口时触发未保存保护。
- 直接改 JOB、批次名与使用专用重命名操作得到一致的关联结果。
- 六表都显示合同客户，客户/JOB 筛选切表后继续生效；回款覆盖可按批次一次勾选多台设备。
- 六个业务标签页均使用独立录入窗口；付款条件整组维护，设备/批次/开票/回款在选择 JOB 后只显示同一 JOB 的关联选项。
- 开票/回款款类同时受前端下拉与后端保存校验保护；逐台勾选制造番号会自动回写批次，整批覆盖按实际设备行计算台数。
- XLSX 输出路径跟随数据库自动确定，避免选择错误文件或把导出物误当数据源。
- UI 采用参考站的 Noto Sans 字体栈、Kanken 蓝/青绿/近黑配色；表格保持紧凑行高、固定表头、明显焦点与选中状态。
- 报价编辑页以左侧可搜索列表 + 右侧表单呈现；来源 JOB 自动带入客户、担当者、型号、数量、未税单价、地址与条款，无设备明细的订单仍带入合同型号供补价。
- 报价 Excel 采用两份示范共有的 A:I 九列、A4 横向、金额公式与动态商务条款结构；示范中的客户抬头、图片和专属文字不会被写死。

## 实际数据演练

- 2026-09-04 项目权威库：合同 40、付款条件 87、设备 615、发货批次 134、开票 102、回款 119；报价表当前为空，等待后续正式录入。
- 数据库 schema v4；`PRAGMA foreign_key_check` 无结果，`PRAGMA integrity_check = ok`，WAL 已完整 checkpoint。
- 从该数据库生成 42 个工作表，业务校验错误 0；数据库与台账均保存在 `BKTC_Ledger/data/`。
- 跨表未回款口径复核：返回 22 个未回款 JOB、70 条未回款明细、合计 25,621,780.58。

## 仍需人工判断的业务风险

- 开票/回款允许一笔覆盖多个批次或制造番号，因此不强行拆成数据库多对多明细；覆盖范围仍需营业人员确认。
- 新建订单的开票/回款空占位是刻意设计；同步飞书/钉钉前应填完或删除空占位。
- 未配置付款比例且无设备的订单只能按“已开票减已回款”显示待确认金额，无法凭空推导合同总额。
- 桌面应用当前未做代码签名；首次启动系统信誉提示与数据可靠性无关。

## 验证命令

```bash
python -m unittest discover -s tests -v
python -m py_compile app.py core.py database.py quotation.py build_ledger_main.py
node --check ui/app.js
```
